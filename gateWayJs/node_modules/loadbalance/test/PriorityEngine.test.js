var expect = require('chai').expect
var RandomEngine = require('../lib/PriorityEngine')

describe('PriorityEngine', function () {

	it('pick items based on priority that is expressed by their weights', () => {

	})
})